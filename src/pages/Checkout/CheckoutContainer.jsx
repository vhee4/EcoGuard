import CheckoutView from "./CheckoutView";
import { Landing } from "../../components";

export const CheckoutContainer = () => {
  return (
    <Landing>
      <CheckoutView />
    </Landing>
  );
};
